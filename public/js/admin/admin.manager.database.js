$(document).ready(function() {





    //global.userSchema.mgBm.find().forEach(function(marcas) { print("Marca: " + marca.nome); });
    // --- Parking Lots --- \\


    // --- Users --- \\


    // --- Personal vehicles --- \\


    // --- Brands and Models --- \\
    // Create a new brand and model
    /*$('#btnBmFormSubmit').click(function(e) {
        e.preventDefault();
        if ('#inptBmModel' == "" || '#inptBmBrand' == "") {
            console.log("Acabou.");
        }
        if ('#inptBmModel' != "" && '#inptBmBrand' == "") {
            console.log("po crl");
        }
    });
    */
});
